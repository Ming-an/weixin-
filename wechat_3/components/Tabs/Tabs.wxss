/* components/Tabs/Tabs.wxss */
page{
  --themeColor:rgb(236, 78, 78);
}
.tabs_title{
  display: flex;
}
.title_item{
  display: flex;
  justify-content: center;
  align-items: center;
  flex: 1;
  padding: 15rpx 0;
}
.active{
  color: var(--themeColor);
  border-bottom: 1rpx solid currentColor;
}